#include <stdio.h>
#include <sys/types.h>
#include <sys/stat.h>
#include <unistd.h>

int main() {
    struct stat st;
    if (stat("file.txt", &st) == 0) {
        printf("File size: %ld bytes\n", st.st_size);
    } else {
        perror("stat");
        return 1;
    }
    return 0;
}
